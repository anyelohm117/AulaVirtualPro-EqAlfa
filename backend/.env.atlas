# ─── MongoDB ATLAS (nube) ────────────────────────────────────
# Copia este archivo como .env para usar Atlas
#   cp .env.atlas .env
# Luego reemplaza la URI con la de tu cluster

PORT=5000
MONGO_URI=mongodb+srv://ADMIN:ADMIN@clusterjadla.mlybpe1.mongodb.net/?retryWrites=true&w=majority&appName=ClusterJADLA
JWT_SECRET=aulavirtual_atlas_secret_2024
JWT_EXPIRES_IN=7d
TEST_URL=http://localhost:5000
